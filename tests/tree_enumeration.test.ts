const treeEnumerationModule = require('../src/tree_enumeration.ts');
const getTreeNodes = treeEnumerationModule.getTreeNodes;

describe('getTreeNodes', () => {
  //let testObject: ;

  afterEach(() => {
    //testObject;
  });

  test('Correct One-level Object', () => {
  
  });
});